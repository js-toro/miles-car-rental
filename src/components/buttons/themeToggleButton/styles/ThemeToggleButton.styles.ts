import styled from 'styled-components';

export const Button = styled.button`
	width: 22px;
	height: 22px;
	margin-left: auto;

	img {
		filter: invert(0.4) brightness(1) sepia(1) hue-rotate(315deg) saturate(5);
	}
`;
